# 테이블 정의서 (shopdb2)

- 버전: 0.1v (초안)
- 작성일시: 2026-09-14 11:42
- 근거 파일: `backup/2026.09.14_backup.sql` (MySQL 8.0.29, HeidiSQL 12.21.0.7344 덤프)
- DB: `shopdb2` (utf8mb4, utf8mb4_0900_ai_ci)
- 총 테이블 수: **31개**

이 문서는 백업 SQL의 `CREATE TABLE` 구문을 그대로 근거로 작성한 데이터 딕셔너리입니다. 스키마 해석이나 개선안은 포함하지 않고, 현재 구조를 있는 그대로 기록합니다.

---

## 도메인 구성

| 도메인 | 테이블 수 | 테이블 목록 |
|---|---|---|
| 1. 사용자/조직 | 6 | org_units, users, roles, user_roles, user_addresses, seller_profiles |
| 2. 상품 | 6 | categories, products, product_variants, product_images, product_files, inventories |
| 3. 주문/결제 | 5 | orders, order_items, payments, payment_transactions, payment_webhook_events |
| 4. 환불 | 3 | refund_policies, refund_requests, refund_items |
| 5. 파일 | 4 | file_assets, policy_files, inquiry_files, rag_document_files |
| 6. 문의/정책 | 2 | buyer_inquiries, company_policies |
| 7. AI/RAG | 5 | ai_providers, rag_documents, rag_chunks, rag_embeddings, rag_query_logs |

---

## 1. 사용자/조직 도메인

### org_units — 조직 단위 (본사/지사/매장/창고)
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| org_id | bigint | PK, AUTO_INCREMENT | 조직 ID |
| parent_org_id | bigint | FK → org_units.org_id | 상위 조직 (자기참조, 계층 구조) |
| org_code | varchar(50) | NOT NULL, UNIQUE | 조직 코드 |
| org_name | varchar(150) | NOT NULL | 조직명 |
| org_type | enum | NOT NULL | HEADQUARTER / BRANCH / STORE / WAREHOUSE |
| business_number | varchar(30) | | 사업자등록번호 |
| representative_name | varchar(100) | | 대표자명 |
| phone | varchar(30) | | 전화번호 |
| email | varchar(255) | | 이메일 |
| zipcode | varchar(20) | | 우편번호 |
| address1 / address2 | varchar(300) | | 주소 |
| active_yn | char(1) | 기본 'Y' | 활성 여부(소프트 삭제) |
| created_at / updated_at | datetime | | 생성/수정일시 |

### users — 사용자 계정
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| user_id | bigint | PK, AUTO_INCREMENT | 사용자 ID |
| org_id | bigint | FK → org_units.org_id | 소속 조직 |
| login_id | varchar(100) | NOT NULL, UNIQUE | 로그인 ID |
| password_hash | varchar(255) | NOT NULL | 비밀번호 해시 |
| user_name | varchar(100) | NOT NULL | 사용자명 |
| email | varchar(255) | NOT NULL, UNIQUE | 이메일 |
| phone | varchar(30) | | 전화번호 |
| user_status | enum | 기본 ACTIVE | ACTIVE / INACTIVE / SUSPENDED / WITHDRAWN |
| created_at / updated_at | datetime | | 생성/수정일시 |

### roles — 역할 정의
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| role_id | bigint | PK, AUTO_INCREMENT | 역할 ID |
| role_code | varchar(30) | NOT NULL, UNIQUE | BUYER / SELLER / ADMIN |
| role_name | varchar(100) | NOT NULL | 역할명(한글) |
| description | varchar(500) | | 설명 |

### user_roles — 사용자-역할 매핑 (N:M)
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| user_id | bigint | PK(복합), FK → users.user_id | 사용자 ID |
| role_id | bigint | PK(복합), FK → roles.role_id | 역할 ID |
| assigned_at | datetime | | 역할 부여일시 |

### user_addresses — 사용자 배송지
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| address_id | bigint | PK, AUTO_INCREMENT | 주소 ID |
| user_id | bigint | FK → users.user_id | 사용자 |
| address_name | varchar(100) | | 배송지명 |
| receiver_name / receiver_phone | varchar | | 수령인 정보 |
| zipcode / address1 / address2 | varchar | | 주소 |
| default_yn | char(1) | 기본 'N' | 기본 배송지 여부 |
| created_at | datetime | | 생성일시 |

### seller_profiles — 판매자 프로필 (users와 1:1)
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| seller_id | bigint | PK, AUTO_INCREMENT | 판매자 ID |
| user_id | bigint | FK → users.user_id, UNIQUE | 사용자 (1:1) |
| company_name | varchar(200) | NOT NULL | 상호명 |
| business_number | varchar(30) | | 사업자등록번호 |
| representative_name | varchar(100) | | 대표자명 |
| settlement_bank / settlement_account | varchar(100) | | 정산 계좌 정보 |
| seller_status | varchar(30) | 기본 ACTIVE | 판매자 상태 |
| created_at | datetime | | 생성일시 |

---

## 2. 상품 도메인

### categories — 카테고리 (계층형)
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| category_id | bigint | PK, AUTO_INCREMENT | 카테고리 ID |
| parent_category_id | bigint | FK → categories.category_id | 상위 카테고리 (자기참조) |
| category_name | varchar(100) | NOT NULL | 카테고리명 |
| category_level | int | 기본 1 | 계층 레벨 |
| display_order | int | 기본 0 | 노출 순서 |
| active_yn | char(1) | 기본 'Y' | 활성 여부 |

### products — 상품
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| product_id | bigint | PK, AUTO_INCREMENT | 상품 ID |
| seller_user_id | bigint | FK → users.user_id | 판매자 |
| category_id | bigint | FK → categories.category_id | 카테고리 |
| product_code | varchar(50) | NOT NULL, UNIQUE | 상품 코드 |
| product_name | varchar(200) | NOT NULL | 상품명 |
| short_description | varchar(1000) | | 짧은 설명 |
| description | longtext | | 상세 설명 |
| regular_price / sale_price | decimal(15,2) | NOT NULL | 정가/판매가 |
| product_status | enum | 기본 READY | READY / SALE / SOLD_OUT / STOPPED / DELETED |
| created_at / updated_at | datetime | | 생성/수정일시 |

### product_variants — 상품 옵션(SKU)
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| variant_id | bigint | PK, AUTO_INCREMENT | 옵션 ID |
| product_id | bigint | FK → products.product_id | 상품 |
| sku_code | varchar(100) | NOT NULL, UNIQUE | SKU 코드 |
| option_name1/value1, option_name2/value2 | varchar(100) | | 옵션명/값 (최대 2단) |
| additional_price | decimal(15,2) | 기본 0.00 | 추가 금액 |
| active_yn | char(1) | 기본 'Y' | 활성 여부 |

### product_images — 상품 이미지
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| product_image_id | bigint | PK, AUTO_INCREMENT | 이미지 ID |
| product_id | bigint | FK → products.product_id | 상품 |
| file_id | bigint | FK → file_assets.file_id | 파일 |
| image_type | enum | 기본 DETAIL | MAIN / DETAIL / THUMBNAIL / OPTION |
| alt_text | varchar(500) | | 대체 텍스트 |
| display_order | int | 기본 0 | 노출 순서 |
| active_yn | char(1) | 기본 'Y' | 활성 여부 |
| created_at | datetime | | 생성일시 |

### product_files — 상품 첨부파일 (매뉴얼 등)
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| product_file_id | bigint | PK, AUTO_INCREMENT | 상품파일 ID |
| product_id | bigint | FK → products.product_id | 상품 |
| file_id | bigint | FK → file_assets.file_id | 파일 |
| file_category | varchar(50) | | 파일 분류 |
| file_description | varchar(500) | | 설명 |
| display_order | int | 기본 0 | 노출 순서 |
| created_at | datetime | | 생성일시 |

### inventories — 재고
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| inventory_id | bigint | PK, AUTO_INCREMENT | 재고 ID |
| org_id | bigint | FK → org_units.org_id | 재고 보유 조직 |
| variant_id | bigint | FK → product_variants.variant_id | 옵션(SKU) |
| stock_quantity | int | NOT NULL, 기본 0 | 재고 수량 |
| reserved_quantity | int | NOT NULL, 기본 0 | 예약(주문 대기) 수량 |
| safety_stock | int | NOT NULL, 기본 0 | 안전 재고 |
| updated_at | datetime | ON UPDATE CURRENT_TIMESTAMP | 수정일시 |
| UNIQUE(org_id, variant_id) | | | 조직-옵션 조합 유일 |

---

## 3. 주문/결제 도메인

### orders — 주문
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| order_id | bigint | PK, AUTO_INCREMENT | 주문 ID |
| order_no | varchar(64) | NOT NULL, UNIQUE | 주문번호 |
| buyer_user_id | bigint | FK → users.user_id | 구매자 |
| org_id | bigint | FK → org_units.org_id | 주문 처리 조직 |
| order_status | enum | 기본 ORDERED | ORDERED/PAYMENT_PENDING/PAID/PREPARING/SHIPPING/DELIVERED/COMPLETED/CANCELLED/REFUNDED |
| product_amount / discount_amount / shipping_amount / total_amount | decimal(15,2) | | 금액 정보 |
| receiver_name / receiver_phone / zipcode / shipping_address1/2 | varchar | | 배송 정보 |
| ordered_at / updated_at | datetime | | 주문/수정일시 |

### order_items — 주문 상세
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| order_item_id | bigint | PK, AUTO_INCREMENT | 주문상세 ID |
| order_id | bigint | FK → orders.order_id | 주문 |
| product_id | bigint | FK → products.product_id | 상품 |
| variant_id | bigint | FK → product_variants.variant_id | 옵션 |
| product_name_snapshot / sku_snapshot | varchar | | 주문 시점 상품명/SKU 스냅샷 |
| quantity | int | NOT NULL | 수량 |
| unit_price / item_amount | decimal(15,2) | NOT NULL | 단가/금액 |
| item_status | varchar(30) | 기본 ORDERED | 항목 상태 |

### payments — 결제
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| payment_id | bigint | PK, AUTO_INCREMENT | 결제 ID |
| order_id | bigint | FK → orders.order_id | 주문 |
| pg_provider | varchar(50) | NOT NULL | PG사 (TOSS/KAKAOPAY 등) |
| payment_key | varchar(255) | UNIQUE | PG 결제키 |
| pg_order_id / customer_key | varchar(255) | | PG 주문ID/고객키 |
| payment_type / payment_method / payment_status | varchar | | 결제 유형/수단/상태 |
| requested_amount / approved_amount / cancelled_amount / balance_amount | decimal(15,2) | | 금액 정보 |
| currency | varchar(10) | 기본 KRW | 통화 |
| receipt_url | varchar(2000) | | 영수증 URL |
| requested_at / approved_at / cancelled_at / created_at | datetime | | 시각 정보 |

### payment_transactions — 결제 트랜잭션 이력
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| transaction_id | bigint | PK, AUTO_INCREMENT | 트랜잭션 ID |
| payment_id | bigint | FK → payments.payment_id | 결제 |
| transaction_key | varchar(255) | | 트랜잭션 키 |
| transaction_type | enum | NOT NULL | REQUEST/APPROVE/CANCEL/PARTIAL_CANCEL/REFUND |
| transaction_status | varchar(50) | | 상태 |
| transaction_amount | decimal(15,2) | NOT NULL | 금액 |
| pg_transaction_id / idempotency_key | varchar(255) | | PG 트랜잭션ID/멱등키 |
| cancel_reason | varchar(500) | | 취소 사유 |
| request_json / response_json | json | | PG 요청/응답 원문 |
| created_at | datetime | | 생성일시 |

### payment_webhook_events — 결제 웹훅 이벤트
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| webhook_id | bigint | PK, AUTO_INCREMENT | 웹훅 ID |
| payment_id | bigint | FK → payments.payment_id | 결제 |
| pg_provider | varchar(50) | | PG사 |
| event_type / event_id | varchar | | 이벤트 유형/ID |
| payload_json | json | | 웹훅 원문 |
| processed_yn | char(1) | 기본 'N' | 처리 여부 |
| error_message | text | | 오류 메시지 |
| received_at / processed_at | datetime | | 수신/처리 시각 |

---

## 4. 환불 도메인

### refund_policies — 환불 정책 (연도별)
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| refund_policy_id | bigint | PK, AUTO_INCREMENT | 정책 ID |
| org_id | bigint | FK → org_units.org_id | 적용 조직 |
| policy_name | varchar(200) | NOT NULL | 정책명 |
| allowed_days | int | NOT NULL | 환불 허용 일수 |
| unopened_refund_yn / opened_refund_yn / defective_refund_yn | char(1) | | 미개봉/개봉/불량 환불 가능 여부 |
| shipping_fee_payer | enum | 기본 BUYER | BUYER/SELLER/COMPANY |
| refund_policy_text | longtext | | 정책 본문 |
| policy_json | json | | 정책 메타(JSON) |
| effective_from / effective_to | date | | 적용 기간 |
| active_yn | char(1) | 기본 'Y' | 활성 여부 |

### refund_requests — 환불 요청
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| refund_request_id | bigint | PK, AUTO_INCREMENT | 환불요청 ID |
| order_id | bigint | FK → orders.order_id | 주문 |
| buyer_user_id | bigint | FK → users.user_id | 구매자 |
| refund_policy_id | bigint | FK → refund_policies.refund_policy_id | 적용 정책 |
| refund_reason | varchar(500) | | 환불 사유 |
| requested_amount / approved_amount | decimal(15,2) | | 요청/승인 금액 |
| refund_status | enum | 기본 REQUESTED | REQUESTED/REVIEWING/APPROVED/REJECTED/COMPLETED |
| requested_at / approved_at / completed_at | datetime | | 시각 정보 |

### refund_items — 환불 상세 항목
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| refund_item_id | bigint | PK, AUTO_INCREMENT | 환불상세 ID |
| refund_request_id | bigint | FK → refund_requests.refund_request_id | 환불 요청 |
| order_item_id | bigint | FK → order_items.order_item_id | 주문 상세 |
| refund_quantity | int | NOT NULL | 환불 수량 |
| refund_amount | decimal(15,2) | NOT NULL | 환불 금액 |

---

## 5. 파일 도메인

### file_assets — 통합 파일 저장소
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| file_id | bigint | PK, AUTO_INCREMENT | 파일 ID |
| org_id | bigint | FK → org_units.org_id | 소속 조직 |
| file_type | enum | NOT NULL | IMAGE/PDF/DOCUMENT/VIDEO/AUDIO/ETC |
| storage_type | enum | NOT NULL | LOCAL/S3/GCS/NAS/URL |
| original_file_name / stored_file_name | varchar(500) | | 원본/저장 파일명 |
| file_extension / mime_type | varchar | | 확장자/MIME 타입 |
| file_size | bigint | 기본 0 | 파일 크기 |
| storage_path | varchar(1000) | | 저장 경로 |
| public_url / thumbnail_url | varchar(2000) | | 공개 URL / 썸네일 URL |
| checksum_sha256 | varchar(64) | | 체크섬 |
| active_yn | char(1) | 기본 'Y' | 활성 여부 |
| created_at | datetime | | 생성일시 |

### policy_files — 정책-파일 연결
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| policy_file_id | bigint | PK, AUTO_INCREMENT | ID |
| policy_id | bigint | FK → company_policies.policy_id | 정책 |
| file_id | bigint | FK → file_assets.file_id | 파일 |
| display_order | int | 기본 0 | 노출 순서 |

### inquiry_files — 문의-파일 연결
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| inquiry_file_id | bigint | PK, AUTO_INCREMENT | ID |
| inquiry_id | bigint | FK → buyer_inquiries.inquiry_id | 문의 |
| file_id | bigint | FK → file_assets.file_id | 파일 |
| created_at | datetime | | 생성일시 |

### rag_document_files — RAG 문서-파일 연결
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| rag_document_file_id | bigint | PK, AUTO_INCREMENT | ID |
| document_id | bigint | FK → rag_documents.document_id | RAG 문서 |
| file_id | bigint | FK → file_assets.file_id | 파일 |

---

## 6. 문의/정책 도메인

### buyer_inquiries — 구매자 문의
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| inquiry_id | bigint | PK, AUTO_INCREMENT | 문의 ID |
| user_id | bigint | FK → users.user_id | 작성자 |
| org_id | bigint | FK → org_units.org_id | 관련 조직 |
| category_code | varchar(50) | NOT NULL | 문의 분류 |
| title | varchar(200) | NOT NULL | 제목 |
| content | text | NOT NULL | 내용 |
| inquiry_status | varchar(30) | NOT NULL | 상태 |
| secret_yn | varchar(1) | NOT NULL | 비밀글 여부 |
| answer_content | text | | 답변 내용 |
| answered_by_user_id | bigint | FK → users.user_id | 답변자 |
| created_at / updated_at / answered_at | datetime | | 시각 정보 |

### company_policies — 회사 정책 (버전 관리)
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| policy_id | bigint | PK, AUTO_INCREMENT | 정책 ID |
| org_id | bigint | FK → org_units.org_id | 적용 조직 |
| policy_code | varchar(50) | NOT NULL | 정책 코드 |
| policy_name | varchar(200) | NOT NULL | 정책명 |
| policy_version | varchar(30) | NOT NULL | 버전 (UNIQUE with policy_code) |
| policy_type | varchar(50) | | 정책 유형 |
| policy_content | longtext | | 정책 본문 |
| effective_from / effective_to | date | | 적용 기간 |
| active_yn | char(1) | 기본 'Y' | 활성 여부 |
| created_at | datetime | | 생성일시 |

---

## 7. AI/RAG 도메인

### ai_providers — AI 프로바이더 설정
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| provider_id | bigint | PK, AUTO_INCREMENT | 프로바이더 ID |
| provider_code | varchar(50) | NOT NULL, UNIQUE | OPENAI/GEMINI/OLLAMA 등 |
| provider_name | varchar(100) | NOT NULL | 프로바이더명 |
| provider_type | enum | NOT NULL | CLOUD / LOCAL |
| base_url | varchar(1000) | | API Base URL |
| chat_model / embedding_model | varchar(200) | | 사용 모델 |
| active_yn | char(1) | 기본 'Y' | 활성 여부 |
| created_at | datetime | | 생성일시 |

### rag_documents — RAG 원본 문서
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| document_id | bigint | PK, AUTO_INCREMENT | 문서 ID |
| provider_id | bigint | FK → ai_providers.provider_id | 임베딩 프로바이더 |
| org_id | bigint | FK → org_units.org_id | 소속 조직 |
| document_type | varchar(50) | | 문서 유형 (REFUND_POLICY 등) |
| document_name | varchar(255) | NOT NULL | 문서명 |
| source_type | enum | | DATABASE/FILE/URL/API/MANUAL |
| source_uri | varchar(2000) | | 원본 위치 |
| content_text | longtext | | 원본 텍스트 |
| version | varchar(50) | | 버전 |
| document_status | enum | 기본 READY | READY/PROCESSING/INDEXED/ERROR |
| created_at / updated_at | datetime | | 시각 정보 |

### rag_chunks — 문서 청크
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| chunk_id | bigint | PK, AUTO_INCREMENT | 청크 ID |
| document_id | bigint | FK → rag_documents.document_id | 원본 문서 |
| chunk_no | int | NOT NULL | 청크 순번 (UNIQUE with document_id) |
| chunk_text | longtext | NOT NULL | 청크 텍스트 |
| token_count | int | | 토큰 수 |
| metadata_json | json | | 메타데이터 |
| created_at | datetime | | 생성일시 |

### rag_embeddings — 임베딩 벡터 메타
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| embedding_id | bigint | PK, AUTO_INCREMENT | 임베딩 ID |
| chunk_id | bigint | FK → rag_chunks.chunk_id | 청크 |
| embedding_provider / embedding_model | varchar | | 임베딩 생성 프로바이더/모델 |
| embedding_dimension | int | | 임베딩 차원 |
| embedding_json | json | | 임베딩 값(직접 저장 시) |
| vector_db_type / vector_collection / vector_external_id | varchar | | 외부 벡터DB(Qdrant 등) 연동 정보 |
| created_at | datetime | | 생성일시 |

### rag_query_logs — RAG 질의응답 로그
| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| query_log_id | bigint | PK, AUTO_INCREMENT | 로그 ID |
| user_id | bigint | FK → users.user_id | 질의 사용자 |
| provider_id | bigint | FK → ai_providers.provider_id | 사용 프로바이더 |
| question_text / response_text | text/longtext | | 질문/응답 |
| retrieved_chunk_ids | json | | 검색된 청크 ID 목록 |
| prompt_tokens / completion_tokens | int | 기본 0 | 토큰 사용량 |
| response_time_ms | int | | 응답 시간(ms) |
| created_at | datetime | | 생성일시 |

---

## 전체 FK 관계 요약

```
org_units (self: parent_org_id)
 └─ users.org_id
     ├─ user_roles.user_id ── roles.role_id
     ├─ user_addresses.user_id
     ├─ seller_profiles.user_id
     ├─ products.seller_user_id
     ├─ orders.buyer_user_id
     ├─ buyer_inquiries.user_id / answered_by_user_id
     ├─ refund_requests.buyer_user_id
     └─ rag_query_logs.user_id
 ├─ file_assets.org_id
 ├─ company_policies.org_id
 ├─ refund_policies.org_id
 ├─ buyer_inquiries.org_id
 ├─ rag_documents.org_id
 └─ inventories.org_id / orders.org_id

categories (self: parent_category_id)
 └─ products.category_id
     ├─ product_variants.product_id
     │   └─ inventories.variant_id
     │   └─ order_items.variant_id
     ├─ product_images.product_id ── file_assets.file_id
     ├─ product_files.product_id ── file_assets.file_id
     └─ order_items.product_id

orders
 ├─ order_items.order_id
 ├─ payments.order_id
 │   ├─ payment_transactions.payment_id
 │   └─ payment_webhook_events.payment_id
 └─ refund_requests.order_id
     ├─ refund_items.refund_request_id ── order_items.order_item_id
     └─ refund_policies.refund_policy_id

buyer_inquiries ── inquiry_files.inquiry_id ── file_assets.file_id
company_policies ── policy_files.policy_id ── file_assets.file_id

ai_providers
 ├─ rag_documents.provider_id
 │   ├─ rag_chunks.document_id
 │   │   └─ rag_embeddings.chunk_id
 │   └─ rag_document_files.document_id ── file_assets.file_id
 └─ rag_query_logs.provider_id
```
